// OO >> dados que manipulam funcoes
objeto = {
    valor1,
    valor2,
    valor3
    processamento () {
       return valor1 + valor2 + valor3
    }
}
objeto.processamento()  //invocamos funcoes a partir de dados

// objeto : agrupam comportamentos e atributos